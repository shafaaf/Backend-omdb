# Copy this file to .env and fill in real values. .env is gitignored; this file is tracked.

# Any long random string (e.g. `openssl rand -base64 48`). Signs/verifies access JWTs.
JWT_SECRET=

# Free key from https://www.omdbapi.com/apikey.aspx — used to fetch and cache movie data.
OMDB_API_KEY=
